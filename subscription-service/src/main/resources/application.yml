server:
  port: 8087   # pick a free port

spring:
  application:
    name: subscription-service

  datasource:
    url: jdbc:mysql://localhost:3306/subscriptiondb
    username: root
    password: YourNewPassword123!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:          
      show-details: always