server:
  port: 8094

spring:
  application:
    name: email-service

  mail:
    host: smtp.gmail.com
    port: 587
    username: srivilliamsai@gmail.com
    password: omyk fiel wbnt pbnb  
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    default-encoding: UTF-8

  datasource:
    url: jdbc:mysql://localhost:3306/emaildb
    username: root
    password: YourNewPassword123!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always