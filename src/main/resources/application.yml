server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zidiodb
    username: root
    password: YourNewPassword123!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect

  application:
    name: zidioconnect
